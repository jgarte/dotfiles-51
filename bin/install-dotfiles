#! /bin/sh

export GIT_DIR=$HOME/.dotfiles
export GIT_WORK_TREE=$HOME

echo "Warning: will overwrite existing files. Continue? Y/n"
[[ $(read answer) != @(Y|y|yes) ]] && exit  
echo "Continued"
# git checkout --force # will overwrite existing files
